recall: for example for recall 3 then how many examples out of let us say you run 100 tests and 50 cases were a hit in top 3 so this is 50% recall
precision: what Proportion of the chunks are good
    this is not too important because even if some chunks are not relevant then it is still okay as long as some chunks are relevant

10 Rag techniques
    1-Chunking R & D: experiment with chunking strategy
    2-Encoder R & D: select the best Encoder model based on a test set
        -there are also images, there are encoders that are good taking images and text and map them into consistent vector space
        -or instead use a modal to write a caption for the image so a question will most likely match and then you vectorize this text (this would work better according to the instructor)
        -there are modals that can work with pdf and word docs but instead it is better to convert them into markdown
    3-improve prompts: general content, the current date, relevant context and history
    4-Document pre-processing: use an LLM to make the chunk and/or text for encoding
        -let LLM change numbers into meaningful text first
        -symentic chunking -chunk based on meaning of each block of text- (there is symentic chunking in langchain)
    5-Query rewriting: use an LLM to convert the user's question to a RAG query
    6-Query expantion: use an LLM to turn the question into multiple RAG queries
    7-Re-ranking: use an LLM to sub-select from RAG results
        maybe ignore unneeded checks and rank the best chunks to top and remove the rest to not polute the context with a lot of useless chunks
    8-Hierarchical: use an LLM to summarize at multiple levels
    9-Graph RAG: retrieve content closely related to similar documents
    10-Agentic RAG: use Agents for retrieval, combining with Memory and Tools such as SQL
        letting llm decide what to do and in which order